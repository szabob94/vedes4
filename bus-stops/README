Bus stops - a preliminary study for development of Robocar City Emulator
========================================================================
This program gives a list of all the bus services operating in a given city.

Install and Usage
-----------------
autoreconf --install
./configure
make
src/bstops ../../debrecen.osm


if the libosmium headers are installed in a user's home directory, use 
the option AM_CXXFLAGS=-I/home/user/libosmium/include in the configure command:

./configure  AM_CXXFLAGS=-I/home/user/libosmium/include
